<?xml version="1.0" encoding="UTF-8"?>
<masterDetail:UIPage xmlns="http://xamarin.com/schemas/2014/forms" 
                                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                                  xmlns:masterDetail="clr-namespace:NavigationFramework.MasterDetail;assembly=NavigationFramework"
                                  x:Class="IoCFinal.Views.PageView">
  <Grid>
      <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
      </Grid.RowDefinitions>
      <Label Grid.Row="0" Text="{Binding Label}" HorizontalOptions="Center"
             VerticalOptions="Center"
             FontSize="40" />
      <Button Grid.Row="1" Text="To detail"
              FontSize="40" Command="{Binding ToDetailPage}" />
  </Grid>
</masterDetail:UIPage>































