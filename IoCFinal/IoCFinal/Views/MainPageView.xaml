<?xml version="1.0" encoding="utf-8" ?>
<masterDetail:UIPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:masterDetail="clr-namespace:NavigationFramework.MasterDetail;assembly=NavigationFramework"
             x:Class="IoCFinal.Views.MainPageView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center"
               FontSize="40"/>
        <Button Grid.Row="1" Text="To Page"
                FontSize="40" Command="{Binding ToPage}" />
    </Grid>
</masterDetail:UIPage>